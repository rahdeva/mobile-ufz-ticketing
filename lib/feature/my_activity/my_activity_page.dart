import 'package:flutter/material.dart';
import 'package:get/get.dart';

class MyActivityPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('txt_menu_notification'.tr),
      ),
    );
  }
}
